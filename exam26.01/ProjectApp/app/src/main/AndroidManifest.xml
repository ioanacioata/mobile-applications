<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ioana.exam">

    <!-- necessary for the connection to the server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- add name of application for the Manager class -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.IdeaActivity"
            android:label="@string/section1" />
        <activity
            android:name=".ui.ProjectActivity"
            android:label="@string/section2" />
        <activity
            android:name=".ui.IdeaDetailsActivity"
            android:label="@string/details1" />
        <activity android:name=".ui.ProjectDetailsActivity"
            android:label="@string/details2"/>
        <activity android:name=".ui.PromoteActivity"
            android:label="@string/promote"></activity>
    </application>

</manifest>